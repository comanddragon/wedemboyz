# Settings are split into base / development / production.
# The active module is selected via the DJANGO_SETTINGS_MODULE env var,
# e.g. config.settings.development or config.settings.production.
# Nothing to import here — see base.py, development.py, production.py.
